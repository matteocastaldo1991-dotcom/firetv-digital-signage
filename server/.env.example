# Server port
PORT=3000

# Base public URL of the server (used to generate absolute media URLs)
BASE_URL=https://your-domain.example

# Admin API token (change this to a random, secret value)
ADMIN_TOKEN=change_this_to_a_random_secret
